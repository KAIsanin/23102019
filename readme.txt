Тех. задание #1 версия на согласование 
    У нас есть таблицы с курсами валют и справочник валют, которые используются в системе. 

    Таблица с курсами валют называется "Курс", в ней поля: дата, валюта, курс. 
    Пример записи: ’08.21.2019’, ‘EUR’, 73,9766. 

    Мы забираем данные с сайта ЦБ и заполняем эту таблицу каждый день новым курсом. 

    С сайта ЦБ берем такой строчкой: http://www.cbr.ru/scripts/XML_daily.asp?date_req=21.08.2019 . 
    Справочник валют называется "Валюты", в нем есть поле валюта, кодвалюты. Пример записи: ’RUR’, ’R00000’; ’CZK’, ’R01760’. 

    Надо написать код , который будет получать курс валюты, которая у нас используется в системе, каждый день и сохранять его в таблицу "Курс" базы данных. 

    Функцию, которая будет возвращать курс валюты на определенную дату. Функция должна принимать два параметра – валюту и дату курса, а возвращать курс.

     Код разместить в любом репозитории и прислать ссылку на репозиторий. Репозиторий должен быть открыт для просмотра. С уважением, ****** ********** *********  *******@*****-***.** 

Интерпретация #1  тех. задания:
    1)Создать табицу "Курс", поля: "дата", "валюта", "курс". 
    
    2)Создать таблицу "Валюты", поля: "валюта", "код валюты"
    
    3)Написать функцию, которая по указанной дате, загружает данные с http://www.cbr.ru/scripts/XML_daily.asp?date_req=ДАТА и заполняет поля ("дата", "валюта", "курс") в таблице "Курс" 
    Функция GetCbrDataToTableKyrs ( тип входящего аргумента дата (Date) )
    
    4)Написать функцию, которая по указанной дате, загружает данные с http://www.cbr.ru/scripts/XML_daily.asp?date_req=ДАТА и заполняет поля ("валюта", "код валюты") в таблице "Валюты"
    Функция GetCbrData2ToTableValuta ( аргумент дата (Date) )
    
    4)Функцию которая возвращает курс валюты на определённую дату. Функция должна принимать два параметра – валюту и дату курса, а возвращать курс. Т.к. не указаанно откуда то:
    
        а)Функция которая данные возвращает из таблицы "Курс"
        GetTableSingleKyrs(аргумент валюта (String), датакурса (Date))
        
        б)Функция которая возвращаает данные с сайта www.cbr.ru
        GetCbrSingleKyrs(аргумент валюта (String), датакурса (Date))

